# Playground 웹 결제 및 운영자 관리

## 공개 경로

- `/playground/index.html`: 제품 소개, 다운로드, 요금제 안내
- `/playground/checkout.html`: Playground 앱에서 결제를 요청할 때 여는 직접 결제 페이지
- `/playground/manage.html`: 운영자 전용 회원·구매 관리 페이지

결제 페이지와 운영자 페이지는 소개 페이지 및 사이트맵에 링크하지 않으며 `noindex`로 설정한다.
이는 접근 제어가 아니다. 실제 보호는 중앙 Playground API의 회원 인증과 운영자 권한 검사에서 수행한다.

## 배포 환경 변수

- `PG_CHECKOUT_API_BASE`: 동일한 Playground 회원·결제 DB를 사용하는 중앙 pgsvc의 공개 HTTPS 주소
- `PG_CHECKOUT_BILLING_ENABLED`: 토스 자동결제 계약과 실제 테스트가 끝난 경우에만 `1`

토스 시크릿 키, Supabase service role 키, 자동결제 암호화 키는 이 홈페이지 저장소나 브라우저 코드에 넣지 않는다.

## 구매 경로

- 직접 고객: 앱 → 별도 결제 페이지 → 토스페이먼츠 → 직접 결제 이용권
- 크몽 고객: 크몽에서 결제 → 운영자가 주문번호·결제 금액·이용 기간 확인 → 운영자 페이지에서 이용권 부여
- 크몽 구매 계정은 홈페이지 직접 결제를 서버에서 차단한다.
- 유입 경로(검색·직접 영업)는 실제 결제 경로와 별도로 기록한다.
- 크몽 주문번호는 한 계정·한 주문에만 연결하고, 같은 내용의 재요청만 중복 없이 성공 처리한다.

## 운영 전 필수 작업

1. 중앙 pgsvc DB에 `028_launch_membership_discount.sql`, `029_purchase_channels.sql`을 순서대로 검토·적용한다.
2. 중앙 pgsvc를 공개 HTTPS 주소로 배포하고 홈페이지의 `PG_CHECKOUT_API_BASE`에 연결한다.
3. 운영자 계정으로 `/playground/manage.html` 로그인과 회원 조회를 확인한다.
4. 합성 회원으로 크몽 주문 등록·중복 등록·이용권 회수를 확인한다.
5. 서버 시각을 한국시간 기준으로 검증해 2026-10-01 00:00부터 2026-11-30 23:59까지의 직접 결제·자동결제만 50% 할인되고, 2026-12-01 00:00부터 정상가로 전환되는지 확인한다. 회원의 가입일·첫 결제일을 바꿔도 할인 기간이 달라지지 않아야 한다.
6. 자동결제 계약과 첫 결제·갱신·해지·실패 복구를 모두 확인한 뒤에만 `PG_CHECKOUT_BILLING_ENABLED=1`로 바꾼다.
7. 웹 운영자 기능이 실제 운영에서 안정적으로 동작하고 기존 6010 기능이 모두 이관된 뒤 6010 운영 스튜디오 제거를 별도 진행한다.

DB 마이그레이션, 배포, 실결제와 실회원 변경은 이 소스 변경만으로 수행되지 않는다.

## 2026-09-15 병렬 검토 결과

ORCA Claude 작업자 2개로 웹과 서버를 나누어 검토하고 수정했다.
웹 회귀 테스트 20개, pgsvc 모의 테스트 14개와 프런트엔드 타입 검사가 통과했다.
서버 모의 테스트는 `cafe_manager_v2/backend/pgsvc/tests/test_promo_billing.py`에 보관하며
DB·환경파일·토스 호출을 대체한 독립 프로세스로 실행한다.

- 10월 1일 전과 12월 1일 이후는 정상가, 10월·11월만 할인가로 표시하고 계산한다.
- 결제 준비와 승인 사이에 가격이 바뀌면 승인 전에 409로 거절하며, 웹에서 다시 로그인해 새 금액을 확인할 수 있다.
- 승인 결과가 불명확한 통신 실패는 웹의 기존 주문 정보를 보존한다.
- 자동갱신은 건별 청구 시각과 구매 경로를 확인하며, 주문 저장 결과가 없으면 청구하지 않는다.
- 회원관리에서는 처리 중 회원 선택 변경과 이전 회원 주문 응답이 현재 화면을 덮는 상황을 막는다.

운영 전 남은 검토 사항:

- 중앙 API 배포·DB 마이그레이션·웹 배포 및 토스 테스트 환경의 전체 결제 검증.
- 기존 서버의 첫 자동결제 실패 복구, 활성 이용권 재구매 시 남은 기간 처리, 승인 응답 유실 시 토스 결과 재조회.
- 자동갱신을 여러 서버 프로세스에서 실행할 때 중복 청구를 방지할 처리.
- 기존 쿠폰·관리자 이용권 변경과 크몽 주문 이력 간 정합성.
- Vercel의 기존 `routes`와 최상위 `headers` 설정 호환성 및 프록시 경유 로그인 제한 동작.

위 항목을 검증하기 전에는 자동결제 운영 준비 완료로 판단하지 않는다.
